# Default gateway URL the login screen pre-fills (you can still change it
# per-session on the login page - it's stored in this browser's localStorage,
# not baked into the build). Copy this file to .env before running/building
# if you want a different default.
VITE_GATEWAY_URL=http://localhost:8080
